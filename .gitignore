*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Markdown docs are local reference material, not app source — keep only the README.
*.md
!README.md

# Build output / packaged binaries — regenerate via ./gradlew, don't commit.
/dist

# Claude Code local session state, not project source.
.claude/

# Signing secrets — never commit a keystore or its credentials.
keystore.properties
*.jks
*.keystore
